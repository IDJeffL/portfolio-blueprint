import { PossibleTypesMap } from '@apollo/client';
import { WordPressTemplate } from '../getWordPressProps.js';
import { Plugin } from '../hooks/index.js';
export interface FaustConfig {
    templates: {
        [key: string]: WordPressTemplate;
    };
    disableLogging: boolean;
    loginPagePath?: string;
    experimentalPlugins: Plugin[];
    possibleTypes: PossibleTypesMap;
}
export declare function setConfig(_config: FaustConfig): void;
export declare function normalizeConfig(_config: FaustConfig): FaustConfig;
export declare function getConfig(): Partial<FaustConfig>;
